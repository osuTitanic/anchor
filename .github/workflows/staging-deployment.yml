name: Deploy to c.staging.titanic.sh

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    if: github.actor == 'Lekuruu'

    steps:
      - name: Update repository
        run: /titanic/scripts/update_project.sh bancho dev
        working-directory: /titanic

      - name: Rebuild & update container
        run: /titanic/scripts/update_containers.sh anchor
        working-directory: /titanic